//WAP to check weather the number is zero or non zero
#include <stdio.h>
int main()
{
int num;
printf("Enter a number;");
scanf("%D",&num);
if (num==0)
{
    printf("Number is zero");
}
else
{
    printf("number=%dis non zero",num);
}
} 